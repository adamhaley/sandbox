define([
  'jQuery',
  'Underscore',
  'Backbone',
], function($, _, Backbone){
  var testCollection = Backbone.Collection.extend({
	initialize: function(options){

	}
  });

  return testCollection;
});
